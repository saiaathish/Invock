# Codex integration

Invock discovers `codex` on `PATH` and manages the marked Invock block in `~/.codex/config.toml`. Existing bytes are backed up before an atomic update. Uninstall removes only the marked block.
